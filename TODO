better metrics in training (peak ram, marg bpp?)
improve synthetic data / train only on synthetic?
clean code
    * simplify / clean code
find optimizations (impact of agg level?), why is interp so slow?
use tqdm for tensor eval
check that there is some overlap over the anchors (coarse embeddings should not be that important)
still a lot of chunking overhead
optimize geometry neighbours caching
bpp lost because of coarse chunking?
benchmark 1agg -> optim 1agg: remove rope + optimize agg

optimization candidates
    * bound the geometry/model caches for long-lived multi-shape processes
    * reduce repeated interpolation mask indexing and stage gathers
    * measure chunk scheduling overhead independently of GNN inference
    * cap background rANS buffering when overlap is enabled without starving it
