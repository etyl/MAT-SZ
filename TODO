better metrics in training (peak ram, marg bpp?)
eval on small 4d tensor during training
train on 4d data
clean code
    * remove MAT mentions
    * simplify / clean code
[done] chose number of neighbors for agregation (agg_level: caps L1 line length;
       --agg-level flag; ~5.7x infer speedup at level 1 on a 4-D tensor, bit-exact)
use tqdm for tensor eval
